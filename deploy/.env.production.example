# First launch over the server IP (HTTP only).
APP_DOMAIN=203.0.113.10
PUBLIC_ORIGIN=http://203.0.113.10
BASE_PATH=/
VITE_API_URL=

POSTGRES_DB=photonic
POSTGRES_USER=photonic
POSTGRES_PASSWORD=change-this-before-production

LOG_LEVEL=info

ML_MODEL_PATH=/models/surrogate.onnx
ML_MODEL_VERSION=surrogate-v1
CVAE_MODEL_PATH=/models/cvae.pt

# Later cut over to a real hostname and HTTPS:
# APP_DOMAIN=app.example.com
# PUBLIC_ORIGIN=https://app.example.com
